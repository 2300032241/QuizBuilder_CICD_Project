spring:
  datasource:
    url: jdbc:mysql://localhost:3310/quizdb?useSSL=false&allowPublicKeyRetrieval=true
    username: quizuser
    password: quizpass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8081
